# Keep the JIT compiler and CUDA headers on the same minor release as the
# cu130 PyTorch/vLLM wheels. Unconstrained resolution can mix CUDA 13.x minors.
nvidia-cuda-cccl==13.0.85
nvidia-cuda-crt==13.0.88
nvidia-cuda-nvcc==13.0.88
nvidia-cuda-runtime==13.0.96
nvidia-nvvm==13.0.88
